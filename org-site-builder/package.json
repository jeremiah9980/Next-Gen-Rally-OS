{
  "name": "@rally/org-site-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Rally-ORG builder kit: governance intake + config-driven engine + no-code Builder Portal for launching branded org sites (no build step, GitHub Pages ready).",
  "scripts": {
    "dev": "npx serve .",
    "start": "npx serve .",
    "validate": "node scripts/validate-config.js",
    "theme": "node scripts/generate-theme.js",
    "new-org": "node scripts/create-new-org.js",
    "bridge": "node scripts/intake-to-build.js"
  },
  "license": "MIT",
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
